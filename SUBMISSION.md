# SUBMISSION — T07 (인증 + 사용자별 데이터 격리)

세부 근거는 [docs/T07_AUTH_IMPLEMENTATION.md](docs/T07_AUTH_IMPLEMENTATION.md)(구현 설명 6항목)와 [docs/T07_EVIDENCE.md](docs/T07_EVIDENCE.md)(5일 기록 ↔ Git 이력 대조, 계산 검산, 인증/소유권 원시 증거, 실제 화면 캡처 6장)에 있다. 이 문서는 그 요약이다.

## 결과물 주소
- https://doitdiary.vercel.app/

## 소스 주소
- https://github.com/Lui-die-lui/doit_todo

## 짧은 확인 방법 4줄

1. 어디로 가나요: https://doitdiary.vercel.app/ 을 열면 자료 없는 비로그인 메인(장식용 별자리 + "로그인하고 궤도 열기" 버튼)이 시크릿 창에서도 바로 보입니다.
2. 세 단계 안에 무엇을 하나요: "로그인하고 궤도 열기" 클릭 → `/login`에서 이메일/비밀번호 입력(또는 "회원가입" 후 로그인, 또는 "Google로 계속하기") → 제출.
3. 무엇이 보이면 통과인가요: `/dashboard`로 이동해 본인 계획·별자리가 보이고, 로그아웃 상태로 `/plans`·`/tasks`·`/do`·`/see`·`/mypage`에 직접 들어가면 `/login`으로 돌아옵니다. `/mypage`에서 전체 자료를 JSON 파일 하나로 내보낼 수 있고, 그 파일에는 본인 자료만 들어 있습니다.
4. 안 될 때는 무엇이 보이나요: 로그인 실패 시 "이메일 또는 비밀번호가 올바르지 않습니다"라는 동일 문구(존재하지 않는 이메일/틀린 비밀번호 구분 없음)가 뜨고, 저장 실패 시 필드별 오류 문구가 뜨며, 완료 버튼을 연속으로 눌러도 오류 없이 정상 안내만 보입니다.

## AI와 내 판단 3줄

1. AI에게 맡긴 일: Better Auth 통합 코드(인증 서버/미들웨어/세션 헬퍼), 소유권 검사가 들어간 쿼리·Server Action 리팩터링, DB migration SQL, 자동 테스트/증거 스크립트 작성, 5일 사용 기록과 Git 이력을 대조해 근거 문서로 정리하는 작업.
2. 내가 직접 판단한 일: 인증 방식·Google OAuth 설정·비밀값 발급, `/dashboard`를 비로그인 상태에서도 여는 빈 데모 화면으로 둘지, 5일간 실제로 계획을 세우고 할 일을 실행하는 것 자체, 계획 규칙을 실제로 바꿀지 말지, 최종 제출 판단.
3. AI 제안을 따르지 않은 일: 없음(이번 T07 범위 한정) — 다만 AI가 "2~3일차 사이 계획 규칙 변경"을 찾을 때 시간상 가까운 commit(하루 예상시간 입력 방식 변경)을 그대로 인정하지 않고, 조건을 엄격히 대조해 "조건 미충족"이라고 정직하게 보고하도록 요구한 결과가 [T07_EVIDENCE.md](docs/T07_EVIDENCE.md) 2-2에 남아 있다.

## 최종 판정 요약 (2026-09-22 기준)

| 항목 | 판정 |
|---|---|
| 이메일/비밀번호 가입·로그인·로그아웃, Google OAuth | 통과 |
| `/`는 공개(비로그인) 화면, 자료 미노출 | 통과 |
| 미인증 접근 차단(페이지 redirect, API 401) | 통과 |
| 사용자별 데이터 완전 격리(양방향 A↔B) | 통과 |
| DB 세션 + 로그아웃/비밀번호 변경 시 세션 폐기 | 통과 |
| 전체 자료 JSON 내보내기(본인 자료만, 비밀 필드 없음) | 통과 |
| 계정 삭제 + 소유 자료 연쇄 삭제 | 통과 |
| 서로 다른 실제 날짜 5개 사용 기록 | 통과 |
| 1일차 지표(질문/단위/계산식) 사전 고정 | **미충족** — 사전 고정 선언 기록 없음 |
| 2~3일차 사이 계획 규칙 변경 | **미충족** — 조건을 만족하는 Git 이력 없음 |
| 5일 합계/평균과 코드 계산 일치 | 통과 — 코드 직접 실행 + 손계산 + 실제 SEE 화면 캡처(`image/run/see.png`) 3중 대조, 전부 일치 |
| lint / typecheck / test / build | 전부 통과 |
| secret 노출 없음 | 통과 |

## 제출 전 체크리스트

- [x] 인증 없는 `/` 공개 확인 (시크릿 창)
- [x] 로그인 없이 `/plans` 등 접근 시 `/login`으로 redirect
- [x] 미인증 API 401
- [x] A/B 양방향 소유권 테스트 통과 (`npm run test:auth` 12/12)
- [x] 로그아웃 후 이전 세션 거절
- [x] 같은 비밀번호의 두 계정 해시가 서로 다름
- [x] 실제 계획 1개 이상, 해당 계획의 실제 할 일 5개 이상 (plan 3, 13건)
- [x] 사용자가 직접 입력한 실제 실행 기록 3개 이상 (9건, 서로 다른 실제 날짜 5개)
- [x] 새로고침 후 데이터 유지 (DB 세션/자료이므로 자동 충족)
- [x] 전체 데이터 JSON 내보내기 확인, 타 사용자 자료/비밀 필드 없음
- [x] `.env`/시크릿 미노출 확인 (`git grep`, production 번들 스캔)
- [x] 서로 다른 실제 날짜 5개에 대응하는 매일 commit — 5일차(09-22) commit `dd06277`(이 문서 자체를 커밋·push)으로 완료
- [x] 최종 배포(Vercel) URL 확정 — https://doitdiary.vercel.app/ (200 응답, 로그인 게이트 공개, `/api/export` 401 확인)
- [ ] 1일차 지표 사전 고정 / 2~3일차 규칙 변경 — **미충족 상태를 그대로 인지하고 제출할지, 지금 규칙을 고정하고 새 5일을 다시 시작할지는 사용자가 결정해야 함**
- [ ] Google OAuth production redirect 실제 동작 확인 — 실제 Google 계정으로 로그인해봐야 하는 항목이라 사용자가 직접 수행
